# Проект "Сапер" (Minesweeper)

🎮 **[Играть в игру](https://openeeer.github.io/minesweeper_js)**

Проект представляет собой веб-версию классической игры "Сапёр" (Minesweeper), реализованную на чистом JavaScript с использованием IndexedDB для хранения истории игр. Игрок открывает ячейки на прямоугольном поле, избегая мин. Цель игры — открыть все безопасные клетки. Если игрок открывает ячейку с миной, игра заканчивается поражением.

Игра поддерживает интерактивное управление мышью, установку флагов, таймер, анимированный фон с частицами и полную систему воспроизведения сохраненных партий.

## Основные возможности

- **Настраиваемое игровое поле** — выбор ширины (5-50), высоты (5-50) и количества мин
- **Два режима управления** — обычный режим открытия ячеек и режим установки флагов
- **Таймер игры** — отслеживание времени прохождения партии
- **Счетчик мин** — показывает количество оставшихся неотмеченных мин
- **Возможность сдаться** — завершить игру досрочно
- **Анимированный фон** — динамические частицы для улучшения визуального опыта
- **Адаптивный дизайн** — автоматическая подстройка размера ячеек под размер экрана

---

## Правила и управление

### Правила игры

- Игрок открывает ячейки, избегая мин
- Если мины нет, ячейка отображает количество соседних заминированных ячеек (от 1 до 8)
- Если рядом с открытой ячейкой нет мин (0), автоматически открывается область незаминированных ячеек до тех пор, пока не встретится ячейка с цифрой
- Игрок может устанавливать флаги на подозрительные ячейки
- Первый ход всегда безопасен — мины размещаются после первого клика
- Победа достигается при открытии всех безопасных ячеек

### Управление

- **Левый клик** — открыть ячейку
- **Правый клик** — установить/убрать флаг
- **Кнопка 🚩** — переключить режим установки флагов (левый клик будет ставить флаги)
- **Кнопка "Сдаться"** — завершить текущую игру досрочно
- **Изменение параметров** (ширина, высота, мины) — автоматически начинает новую игру

### Цветовая индикация

- **Цифры 1-8** — количество мин в соседних ячейках (каждая цифра имеет свой цвет)
- **Оранжевая ячейка** — взорвавшаяся мина (при проигрыше)
- **Красные ячейки** — все остальные мины (показываются после проигрыша)

---

## Работа с базой данных

Игра автоматически сохраняет все партии в локальную базу данных IndexedDB браузера. Для каждой игры сохраняется:

### Информация об игре

- Дата и время игры
- Имя игрока (опционально)
- Размер игрового поля (ширина × высота)
- Количество мин
- Seed генератора случайных чисел (для точного воспроизведения)
- Результат игры (Победа/Поражение/Сдался)
- Время прохождения в секундах

### Запись ходов

Каждый ход игрока записывается с информацией:
- Временная метка хода
- Тип действия (`reveal` — открытие ячейки, `flag` — установка/снятие флага)
- Координаты ячейки (x, y)
- Значение флага (для действий с флагами)

### История игр

Модальное окно "Сыгранные игры" предоставляет:
- **Таблицу всех сохраненных партий** с полной информацией
- **Сортировку** по любому столбцу (игрок, дата, размер, мины, время, результат)
- **Кнопку "Повторить"** для каждой игры — запускает режим воспроизведения
- **Кнопку "Очистить"** — удаляет всю историю игр

### Воспроизведение партий

При выборе режима "Повторить" игра:
1. Восстанавливает точное расположение мин используя сохраненный seed
2. Воспроизводит игровое поле с исходными параметрами
3. Предоставляет два режима воспроизведения:
   - **▶ Авто** — автоматическое пошаговое воспроизведение всех ходов (0.5 сек между ходами)
   - **⏩ Шаг** — ручное воспроизведение по одному ходу
4. Показывает результат каждого хода в реальном времени
5. Отображает финальный результат партии (Победа/Поражение/Сдался)

---

## Технические особенности

### Архитектура

Проект состоит из модульной структуры:
- **game.js** — основная игровая логика, класс MinesweeperGame
- **app.js** — управление UI, обработка событий, интеграция с БД
- **db.js** — работа с IndexedDB (сохранение/загрузка результатов и ходов)
- **particles.js** — анимация фоновых частиц
- **styles.css** — стили и адаптивный дизайн
- **index.html** — структура приложения

### Генератор случайных чисел

Используется детерминированный генератор на основе алгоритма **xorshift32**:
- Позволяет воспроизводить идентичные игровые поля по seed
- Обеспечивает точное повторение партий при воспроизведении
- Независим от встроенного Math.random()

### Хранилище данных

- **IndexedDB** — локальная NoSQL база данных браузера
- Две таблицы: `results` (результаты игр) и `moves` (ходы)
- Данные сохраняются локально и не требуют сервера
- Поддержка асинхронных операций

### Адаптивность

- Автоматический расчет размера ячеек в зависимости от размера экрана
- Минимальный размер ячейки: 24px
- Максимальный размер ячейки: 40px
- Поддержка полей от 5×5 до 50×50

---

## Требования

1. **Современный браузер** с поддержкой:
   - ES6 модулей (import/export)
   - IndexedDB
   - CSS Grid
   - Async/Await

2. **Рекомендуемые браузеры**:
   - Google Chrome 90+
   - Mozilla Firefox 88+
   - Microsoft Edge 90+
   - Safari 14+

---

## Установка и запуск

### Вариант 1: Онлайн версия (Рекомендуется)

Просто откройте игру в браузере:

🎮 **[https://openeeer.github.io/minesweeper_js](https://openeeer.github.io/minesweeper_js)**

### Вариант 2: Локальный запуск

1. **Склонируйте репозиторий:**

```bash
git clone https://github.com/openeeer/minesweeper_js.git
```

2. **Перейдите в каталог проекта:**

```bash
cd minesweeper_js
```

3. **Запустите локальный сервер:**

Вы можете использовать любой статический веб-сервер, например:

```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server

# PHP
php -S localhost:8000
```

4. **Откройте в браузере:**

```
http://localhost:8000
```

### Примечания

- Игра использует ES6 модули, поэтому требуется запуск через веб-сервер (не работает при открытии файла напрямую через file://)
- Все данные сохраняются локально в браузере
- История игр привязана к домену, на котором запущена игра

---

## Используемые технологии

- **Vanilla JavaScript (ES6+)** — без фреймворков и библиотек
- **IndexedDB API** — для хранения истории игр
- **CSS Grid** — для адаптивной сетки игрового поля
- **Canvas API** — для анимации фоновых частиц
- **Web Storage API** — для локального хранения данных

---

🎮 **[Играть в игру](https://openeeer.github.io/minesweeper_js)**
